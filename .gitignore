# ── OS ──
.DS_Store

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# ── Runtime / local state ──
*.log
.co-studio/

# ── Xcode / Swift (macOS app under macos/) ──
DerivedData/
*.xcuserstate
xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/
*.moved-aside
# xcodebuild -derivedDataPath output (macos/ConnectOnionStudio/)
build-release/

# ── 内部设计 & 开发思考文档：只留本地，永不发布 ──
docs/
DESIGN.md

# Sparkle CLI tools (build-time only)
macos/.sparkle-tools/
macos/ConnectOnionStudio/build-check/

# local release guide (not for GitHub)
/RELEASING.md

# ── Secrets: NEVER commit / push ──
# Sparkle EdDSA signing PRIVATE KEY (release-signing secret). Authoritative copy lives in the
# login Keychain; the file here is only a convenience backup. Bare name → matches in any dir.
sparkle_private_key.txt
*.private_key
*.private.key
